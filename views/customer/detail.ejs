<!DOCTYPE html>
<html>
<head>
  <title>Detail Buku</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-white">
  <div class="container py-5">
    <a href="/" class="btn btn-secondary mb-3">Kembali</a>
    <div class="row">
      <div class="col-md-4">
        <img src="<%= book.imageUrl %>" class="img-fluid">
      </div>
      <div class="col-md-8">
        <h2><%= book.title %></h2>
        <p><strong>Penulis:</strong> <%= book.author %></p>
        <p><strong>Tahun:</strong> <%= book.year %></p>
        <p><strong>Sinopsis:</strong> <%= book.synopsis %></p>
        <% if (book.available) { %>
          <form action="/pinjam/<%= book._id %>" method="POST">
            <div class="mb-2">
              <input type="text" name="nama" placeholder="Nama" class="form-control" required>
              <input type="text" name="nim" placeholder="NIM" class="form-control my-2" required>
              <input type="text" name="kelas" placeholder="Kelas" class="form-control" required>
            </div>
            <button class="btn btn-success">Pinjam Buku</button>
          </form>
        <% } else { %>
          <span class="badge bg-danger">Sedang Dipinjam</span>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
